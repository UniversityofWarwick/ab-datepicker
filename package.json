{
  "name": "@universityofwarwick/ab-datepicker",
  "version": "2.2.0",
  "description": "An accessible and bootstrap compatible datepicker",
  "main": "js/datepicker.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "minify-js": "terser -o js/datepicker.min.js js/datepicker.js --source-map url=datepicker.min.js.map",
    "build": "npm run minify-js",
    "prepublishOnly": "npm run build",
    "lint": "eslint js/datepicker.js",
    "test": "echo \"No automated tests, open test/*.html demos in browser\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eureka2/ab-datepicker.git"
  },
  "keywords": [
    "datepicker",
    "accessible"
  ],
  "license": "MIT",
  "devDependencies": {
    "eslint": "7.5.0",
    "terser": "4.8.0"
  }
}
